  spring:
    datasource:
      url: jdbc:mysql://localhost:3306/study
      username: root
      password:
      driver-class-name: com.mysql.cj.jdbc.Driver
    jpa:
      hibernate:
        ddl-auto: update
      show-sql: true
      properties:
        hibernate:
          format_sql: true
      database: mysql
      database-platform: org.hibernate.dialect.MySQLDialect
#    tiyopasswordkbujheuni
    mail:
      host: smtp.gmail.com
      port: 587
      username: kaamchalau@gmail.com
      password: tiyopasswordkbujeuni
      properties:
        mail:
          smtp:
            auth: true
            starttls:
              enable: true

    cache:
      type: redis
      cache-names: randomNumber

    jackson:
      serialization:
        fail-on-empty-beans: false